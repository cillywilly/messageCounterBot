telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN}
    username: ${TELEGRAM_BOT_USERNAME}
logging:
  level:
    root: "ERROR"
spring:
  datasource:
    url: jdbc:postgresql://db:5432/
    username: "postgres"
    password: "1488_terapia"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
